<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head.ejs') %>

    <body>
        <div id="wrapper">
            <main>
                <% if (title) { %>
                    <h1>
                        <%= title %>
                    </h1>
                    <% } else { res.redirect('/') } %>

                        <%# messages() %>

            </main>

        </div>
        <script src="./js/script.js"></script>
    </body>

</html>